Implement `binary_metrics(tp, tn, fp, fn)` in Python. Counts are non-negative integers. Return a dictionary containing float values for `accuracy`, `precision`, `recall`, `f1`, and `false_positive_rate`. Every metric whose denominator is zero must be `0.0`, including F1 when precision plus recall is zero. Raise `ValueError` for negative or non-integer counts. Return only complete Python source code.

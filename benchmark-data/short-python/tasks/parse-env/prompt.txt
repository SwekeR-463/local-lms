Implement `parse_env(text)` in Python. Ignore blank lines and lines whose first non-whitespace character is `#`. Every other line must contain `=`; split only at the first one, trim whitespace around key and value, and preserve quotes and additional `=` literally. Keys must be non-empty. Return a dictionary where later duplicate keys win. Raise `ValueError` mentioning the 1-based line number for malformed lines. Return only complete Python source code.
